{
  "AppCredentials": {
    "ReUpdateMinutes": 10,
    "ClientInfo": {
      "ClientId": "${client-id}",
      "ClientSecret": "${client-secret}",
      "Audience": "${audience}",
      "Domain": "${domain}"
    }
  }
}